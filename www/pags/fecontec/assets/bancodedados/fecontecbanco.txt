create database fecontecAdmin;
use fecontecAdmin;

create table convidados(
id int NOT NULL AUTO_INCREMENT,
nome varchar(180),
tipoDoc varchar(10),
rm varchar(4),
rg varchar (15),
cursos varchar (20),
presencas varchar (10),
PRIMARY KEY (id) 
); 

select * from convidados;






create table presencas(
rm varchar(4));

 $_preview= $_POST["preview"];

$sql = "INSERT INTO presencas (rm) VALUES ('$_preview')";
if (mysqli_query($conexao, $sql)){
    echo "<script> alert('Dados Salvos '); window.location = 'index.php';
    </script>";
}else{
    echo "Deu erro" . $sql . "<br>" . mysqli_error($conexao);
} 

UPDATE convidados SET presencas=1 WHERE id=3;